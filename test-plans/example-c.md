# Test Plan — Example C (Explicit pages/workflows only, no default count)

Rename to `test-plans/frontend/plan.md` or `test-plans/backend/plan.md` as needed.

---

Test the following pages and workflows only:

- Homepage — verify the main heading and primary navigation are visible
- /about — verify the page loads with meaningful content
- /services — verify service offerings and any category links are visible
- /careers — verify job listing or apply links are present
- /contact — fill out the form fields (name, email, message) and verify the submit button is visible

<!-- No default count — the agent tests exactly these pages and workflows.
     Every listed page automatically gets a VRT screenshot in addition to any functional tests. -->
